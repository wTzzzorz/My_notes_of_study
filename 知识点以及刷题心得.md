# 算法竞赛核心知识库与刷题心法

## 一、 数据结构 

### 1. 树状数组 (BIT)
树状数组是一种基于二进制思想的数据结构，其基本用途是**维护序列的前缀和**。
对于给定的序列 $a$，我们建立一个数组 $c$，其中 $c[x]$ **保存序列 $a$ 的区间 $[x-\text{lowbit}(x)+1, x]$ 中所有数的和**。

> **前置知识：lowbit 定义**
> `lowbit(n)` 定义为非负整数 $n$ 在二进制表示下“最低位的 1 及其后边所有的 0”。

```cpp
// lowbit 求法
int lowbit(int x) { return x & (-x); }
```

**树状数组核心模板：**
```cpp
struct BIT {
    int n;
    vector<int> tree;
    BIT(int n) : n(n), tree(n + 1, 0) {}
    
    int lowbit(int x) { return x & -x; }
    
    // 单点增加
    void add(int x, int d) {
        for (int i = x; i <= n; i += lowbit(i)) tree[i] += d;
    }
    
    // 查询前缀和 [1, x]
    int ask(int x) {
        int sum = 0;
        for (int i = x; i > 0; i -= lowbit(i)) sum += tree[i];
        return sum;
    }
};
```

### 2. 线段树
支持区间修改以及区间查询的高阶数据结构

听完线段树1的讲解后本人认为线段树的Node构造需要遵循

>首先l与r是必须的，其次就是答案让查询的东西，然后我们思考这个东西是否能被递归pushup更新上去，如果不能我们就继续把这个元素拆分成可以递归更新的属性，这便是框架

听完线段树2的讲解后本人心得
> 好他妈麻烦啊啊啊啊啊，首先懒标记太重要，其次如果有多个懒标记需要有一个好用的顺序，方便你写代码修改pushdown和pushup操作

大致模板如下：

```cpp
struct SegTree {
//本模板对应题意，区间修改，区间求和
	struct Node {
		int l, r;
		int sum, add;
	} tr[4 * N];

	vector<int> w;

	void Sread(int n) {
		w.assign(n + 1, 0);
		fors(i, 1, n) cin>>w[i];
	}

	void pushup(Node &u, Node &l, Node &r) { //一份pushup对自己
		u.sum = l.sum + r.sum;
	}

	void pushup(int u) { //对外
		pushup(tr[u], tr[u << 1], tr[u << 1 | 1]);
	}

	void pushdown(Node &u, Node &l, Node &r) { //pushdown懒标记的更新
		l.sum += (l.r - l.l + 1) * u.add;
		r.sum += (r.r - r.l + 1) * u.add;

		l.add += u.add;
		r.add += u.add;

		u.add = 0;
	}

	void pushdown(int u) {
		pushdown(tr[u], tr[u << 1], tr[u << 1 | 1]);
	}

	void build(int u, int l, int r) { //建树
		if (l == r) tr[u] = {l, r, w[l], 0};
		else {
			tr[u].l = l, tr[u].r = r;
			int mid = (l + r) >> 1;
			build(u << 1, l, mid), build(u << 1 | 1, mid + 1, r);
			pushup(u);
		}
	}

	void modify(int u, int l, int r, int d) { //修改
		if (l <= tr[u].l && tr[u].r <= r) {
			tr[u].sum += (tr[u].r - tr[u].l + 1) * d;
			tr[u].add += d;
		}
		else {
			pushdown(u);
			int mid = (tr[u].l + tr[u].r) >> 1;
			if (l <= mid) modify(u << 1, l, r, d);
			if (r > mid) modify(u << 1 | 1, l, r, d);
			pushup(u);
		}

	}
 
	Node query(int u, int l, int r) { //查询
		if (l <= tr[u].l && tr[u].r <= r) return tr[u];

		pushdown(u);
		int mid = (tr[u].l + tr[u].r) >> 1;
		if (r <= mid) return query(u << 1, l, r);
		if (l > mid) return query(u << 1 | 1, l, r);
		else {
			auto left = query(u << 1, l, r);
			auto right = query(u << 1 | 1, l, r);
			Node res;
			pushup(res, left, right);
			return res;
		}
	}
};
```
### 3. 链表模板
```cpp
struct List {
	int e[N], l[N], r[N];
	int idx;

	List () {
		init();
	}

	void init() {
		idx = 2;
		r[0] = 1;
		l[1] = 0;
	}

	void insert_right(int k, int x) {

		e[idx] = x;
		l[r[k]] = idx;
		r[idx] = r[k];
		l[idx] = k;
		r[k] = idx;
		idx++;
	}

	void add_head(int x) {
		insert_right(0, x);
	}

	void add_tail(int x) {
		insert_right(l[1],x);
	}

	void remove(int k) {
		r[l[k]] = r[k];
		l[r[k]] = l[k];
	}

	void insert_left(int k, int x) {
		insert_right(l[k], x);
	}


};

```
### 4. 队列模板
```cpp
int queues[N];
int tt=-1,hh;
void push(int x)
{
	queues[++tt]=x;
}
void pop()
{
	hh++;
}
void query()
{
	cout<<queues[hh]<<"\n";
}
void empty()
{
	if(tt<hh)cout<<"YES"<<"\n";
	else cout<<"NO"<<"\n";
}
```
### 5.栈模板
```cpp
int stk[N],tt = 0;

void add(int x)
{
    stk[++tt] = x;
}
void pop()
{
    tt--;
}
int top()
{
    return stk[tt];
}
bool empty()
{
    return tt==0;
}

```

### 6.Trie
```cpp
int trie[N][26],idx,cnt[N*26];

void insert(string &s){
    int p = 0;
    for(int i=0;i<s.size();i++){
        int u = s[i]-'a';
        
        if(!trie[p][u]) trie[p][u]=idx++;
        
        p = trie[p][u];
    }    
    
    cnt[p]++;
}

int query(string &s){
    int p = 0;
    for(int i=0;i<s.size();i++){
        int u = s[i]-'a';
        if(!trie[p][u]) return 0;
        p=trie[p][u];
    }
    
    return cnt[p];
}
```

### 7.并查集
> 找爹
> 

```cpp
#include<bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10;

int f[N];

int find(int a){
    if(f[a]!=a) f[a]=find(f[a]);
    return f[a];
}

int main(){
    cin.tie(nullptr)->sync_with_stdio(false);
    
    int n,m;
    cin>>n>>m;
    
    for(int i=1;i<=n;i++) f[i]=i;
    
    while(m--){
        string op;
        int a,b;
        cin>>op>>a>>b;
        if(op[0]=='M') f[find(a)]=find(b);
        else{
            if(find(a)==find(b)) cout<<"Yes\n";
            else cout<<"No\n";
        }
    }
    
    return 0;
}
```

### 8.堆
```cpp
#include<bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10;

int h[N],cnt,hp[N],ph[N];

void swap_h(int a,int b){
    swap(ph[hp[a]],ph[hp[b]]);
    swap(hp[a],hp[b]);
    swap(h[a],h[b]);
}

void down(int x){
    int u = x;
    if(x*2<=cnt&&h[u]>h[x*2])u = 2*x;
    if(x*2+1<=cnt&&h[u]>h[x*2+1])u = 2*x+1;
    
    if(u!=x){
        swap_h(u,x);
        down(u);
    }
}

void up(int x){
    while(x/2&&h[x]<h[x/2]){
        swap_h(x,x/2);
        x/=2;
    }
}


int main(){
    int n,m=0;
    cin>>n;
    
    while(n--){
        string op;
        cin>>op;
        if(op[0]=='I'){
            int x;
            cin>>x;
            m++;
            ph[m]=++cnt;
            hp[cnt]=m;
            h[cnt]=x;
            up(cnt);
        }
        else if(op[0]=='P'){
            cout<<h[1]<<'\n';
        }
        else if(op[1]=='M'){
            swap_h(1,cnt);
            cnt--;
            down(1);
        }
        else if(op[0]=='D'){
            int k;
            cin>>k;
            k=ph[k];
            swap_h(k,cnt);
            cnt--;
            down(k);
            up(k);
        }
        else{
            int k,x;
            cin>>k>>x;
            k=ph[k];
            h[k]=x;
            down(k);
            up(k);
        }
    }
    
    return 0;
}
```
---

## 二、 算法思想与策略 

### 1. 二维偏序（扫描线降维打击）
**核心本质**：利用“排序”消灭一个维度，再用数据结构（BIT）动态维护另一个维度。

**通俗理解（HR 招聘模型）**：
假设求职者有两个属性：**笔试成绩 ($X$)** 和 **面试成绩 ($Y$)**。要求找出 $X \le 80$ 且 $Y \le 90$ 的人。
直接嵌套循环会导致 $O(N^2)$。我们的降维魔法如下：
1. **消灭第一维 $X$**：将所有人按笔试成绩 $X$ 从低到高排队。现在我们从左到右挨个审核，审核到第 $k$ 个人时，前面的 $k-1$ 个人 $X$ 必然满足条件！这样 $X$ 维就被**排序动作**消灭了。
2. **维护第二维 $Y$**：准备一个一维的树状数组（黑板）专门记录面试成绩 $Y$。每审核过一个人就把他的 $Y$ 登记上去。查询时，直接在这个一维黑板上求 $Y$ 的前缀和即可。

### 2. 离散化 
当坐标值域极大（如 $10^9$）但元素个数有限（如 $10^5$）时，将数值映射为 $1 \sim N$ 的紧凑排名，以便存入树状数组。

**离散化黄金三连：复制 -> 排序+去重 -> 二分找排名**
```cpp
vector<int> b = a; 
sort(b.begin(), b.end()); 
b.erase(unique(b.begin(), b.end()), b.end()); 

for (int i = 0; i < n; i++) {
    // lower_bound 返回迭代器，减去 begin() 得到 0-based 下标。加 1 变成 BIT 1-based 排名
    a[i] = lower_bound(b.begin(), b.end(), a[i]) - b.begin() + 1; 
}
```

### 3. 归并排序求逆序对
注意右半段较小时才会产生逆序对。

```cpp
int a[N], tmp[N]; 

int merge_sort(int l, int r) {
    if (l >= r) return 0;

    int mid = l + (r - l) / 2; // 【防爆排雷】：不要写 l + r >> 1，注意优先级和防溢出
    int res = merge_sort(l, mid) + merge_sort(mid + 1, r);

    int i = l, j = mid + 1, k = 0;
    while (i <= mid && j <= r) {
        if (a[i] <= a[j]) {
            tmp[k++] = a[i++]; 
        } else {
            // 右边较小，构成逆序对！
            // 因为左半段 [i, mid] 是有序的，a[i] 到 a[mid] 都大于 a[j]
            res += mid - i + 1; 
            tmp[k++] = a[j++];
        }
    }

    while (i <= mid) tmp[k++] = a[i++];
    while (j <= r) tmp[k++] = a[j++];
    for (i = l, k = 0; i <= r; i++, k++) a[i] = tmp[k];

    return res;
}
```

### 4. 懒惰更新与时间戳
面对高频的全局覆盖（`a = d`）操作，绝对不写 $O(n)$ 的硬拷贝。
* **策略**：维护一个全局 `last_reset` 和单点的 `last_change[i]`。
* **致命陷阱**：在对任何数据进行判断之前，必须先校验它的时间戳！如果过期了，必须先“唤醒”恢复真实值，用假数据做判断是 Lazy Update 的大忌。

### 5.数学公式的合理拆分
当你推理出来本题目的数学公式的时候，应当往下一直化简。
* **思想** ： 分离包含有多个变量的项，使公式中不同变量之间互相独立

例子： 
> $\sum_{i=1}^{x}\sum_{j=1}^{i} b_j = \sum_{i=1}^{x}(x-i+1)*b_i$
>
> $\sum_{i=1}^{x}(x-i+1)*b_i = (x+1)\sum_{i=1}^{x}b_i-\sum_{i=1}^{x}i*b_i$
> 
这样一来，该数学式子就可以拆分成两个前缀和，都可以实现

### 6.快速排序板子
```cpp
	auto qsort = [&](auto&&self,int l,int r)->void{
		if(l>=r) return;
		int left = l-1;
		int right = r + 1;
		int base = a[(left+right)>>1];
		
		while(left<right){
			while(a[++left]<base);
			while(a[--right]>base);
			if(left<right) swap(a[left],a[right]);
		}

```

### 7.KMP
```cpp
#include<bits/stdc++.h>
using namespace std;
int ne[1000010];
int main(){
    int a,b;
    string a1,b1;
    cin>>a>>a1>>b>>b1;
    a1 = " "+a1;
    b1 = " "+b1;
    
    for(int i=2,j=0;i<=a;i++){
        while(j&&a1[j+1]!=a1[i]) j=ne[j];
        if(a1[j+1]==a1[i]) j++;
        ne[i]=j;
    }
    
    for(int i=1,j=0;i<=b;i++){
        while(j&&a1[j+1]!=b1[i]) j=ne[j];
        if(a1[j+1]==b1[i]) j++;
        if(j==a){
            cout<<j-a<<" ";
        }
    }
    
    return 0;
}
```

### 8.字符哈希及哈希
```cpp
#include<bits/stdc++.h>
#define ull unsigned long long
using namespace std;
const int N = 1e5+10,P=131;

string s;
ull h[N],p[N];

ull get_val(int l,int r){
    return h[r]-h[l-1]*p[r-l+1];
}

int main(){
    int n,m;
    cin>>n>>m;
    cin>>s;
    s=" "+s;
    p[0]=1;
    for(int i=1;i<=n;i++){
        p[i]=p[i-1]*P;
        h[i]=h[i-1]*P + s[i];
    }
    
    while(m--){
        int a,b,c,d;
        cin>>a>>b>>c>>d;
        
        if(get_val(a,b)==get_val(c,d)) puts("Yes");
        else puts("No");
    }
    
    return 0;
}
```

```cpp
#include<bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10,null = 0x3f3f3f3f;
int h[N*3];

int find(int x){
    int t = (x%N+N)%N;
    while(h[t]!=null&&h[t]!=x){
        t++;
        t=t%(N*3);
    }
    
    return t;
}

int main(){
    memset(h,0x3f,sizeof(h));
    int n;
    cin>>n;
    string op;
    int x;
    while(n--){
        cin>>op>>x;
        if(op[0]=='I') h[find(x)] = x;
        else{
            if(h[find(x)]==null) puts("No");
            else puts("Yes");
        }
    }
    
    return 0;
}
```

---

## 三、 图论与搜索 

### 1. 拆环成链
**核心思想**：如何简洁地表示环中两点距离？开一个二倍数组，将环拆成链子。
`pre[n+i]` 的含义是从起点出发走两圈到该点。例如：顺时针 6 点到 4 点的距离 = **走两圈到 4 点的距离 - 走一圈到 6 点的距离**。

```cpp
// 核心逻辑：s 为第一圈的总周长
for (int i = 1; i <= n; i++) {
    pre[i + n] = pre[i] + s; 
}
```

### 2. DFS / 回溯法
DFS 虽然千变万化，但万变不离其宗。核心在于**保存现场与恢复现场**。

```cpp
void dfs(int step) {
    if (/* 到达终点 或 满足边界条件 */) {
        // 记录答案或输出
        return;
    }
    
    for (/* 遍历当前状态的所有可能选择 */) {
        if (/* 该选择合法且未被访问 */) {
            // 1. 标记该选择已被访问
            // 2. 保存现场 (应用该选择)
            
            dfs(step + 1); // 递归进入下一层
            
            // 3. 恢复现场 (撤销该选择) —— 回溯的核心！
            // 4. 解除访问标记
        }
    }
}
```

---

## 四、 数学与数论 

### 1. 取模极限规律 
**核心定理**：当 $B < A$ 时，对于给定的整数 $A$ 对任意正整数 $B$ 取模，其最大可能取值为：
**$A \bmod B \le \lfloor \frac{A-1}{2} \rfloor$**
*(注：当且仅当 $B = \lfloor \frac{A}{2} \rfloor + 1$ 时取到最大值。如果 $B > A$，则 $A \bmod B = A$)*。

### 2. MEX 
指在一个非负整数集合中**不包含的最小非负整数**。
* 例：集合 $S = \{0, 1, 2, 3\}$，那么 $\operatorname{mex}(S) = 4$。

### 3. 连续整数异或和的周期性质
定义前缀异或和 $P(i) = 1 \oplus 2 \oplus 3 \oplus \dots \oplus i$，规定 $P(0) = 0$。
$P(i)$ 的取值具有以 $4$ 为周期的强规律：
* $P(i) = i$，若 $i \equiv 0 \pmod 4$
* $P(i) = 1$，若 $i \equiv 1 \pmod 4$
* $P(i) = i+1$，若 $i \equiv 2 \pmod 4$
* $P(i) = 0$，若 $i \equiv 3 \pmod 4$

### 4. 哥德巴赫猜想

哥德巴赫猜想的现代陈述为：**任一大于5的整数都可写成三个质数之和。**

由此可以得到两种情况：

1.强哥德巴赫猜想（强哥猜）：**即任一大于2的偶数都可写成两个质数之和**；（未被证明，但是同时也没有被推翻，即在本体的范围内强哥猜成立）

2.弱哥德巴赫猜想（弱哥猜）**:任何一个大于5的奇数都能被表示成三个奇质数的和**。（一个质数可以被多次使用）;（已经被证明） 

### 5. 质数

* 从2开始十个质数相乘就能到int范伟

- 试除法判断质数
- 1e8以内范围质数大概存在60000000，只需要开6000000万就够了
<details>
	<summary> <font color="blue">本人听课再次推理心得</font> </summary>
 	首先就是可以通过暴力枚举呢，从1枚举到n每个都除一遍看看能不能除尽，时间复杂度O（n）
 	但是呢我们想一下是没是n如果能被i除掉得到的结果是不是n/i是不是意味着，约数都是成对出现的，也就	 是我们只需要枚举到n/i即可对不对，然后呢预防这个数是平方数我们要取等于号，时间复杂度也就优化成	 为了O(sqrt(n))
</details>


#### 核心代码
```cpp
	for(int i =2 ;i<=n/i;i++)
		if(n%i==0) return false;
	return true;
```

- 分解质因数
<details>
	<summary> <font color="blue">本人听课再次推理心得</font> </summary>
	首先呢就是暴力枚举，从1枚举到n每个都除一遍去找他的因数<br>由于我们每次都让这个数字除尽，所以我们找的也就是质因数<br>因为你想，从2开始，2的倍数全被除了，3的倍数也没了，对不对以此类推，我们找的其实也就是质因数了<br>但是这个的时间复杂度呢大概是O（n）因为最坏情况就是当前数是质数，否则n就可以被除减小循环次数。<br>
	但是呢我们可以跟上一个试除法一样思想，因数都是成对存在的，所以质因数最大只能是sqrt(n)，所以我们就可以优化成O(sqrt(n))的时间复杂度了这个还是最坏的情况，快的话其实应该会很快因为num一直在减小
</deatils>


#### 核心代码
``` cpp
	int cnt = 0;
	
	for(int i=2;i<=num/i;i++){
		if(num%i==0){
			while(num%i==0){
				num/=i;
				cnt++;
			}
			cout<<i<<" "<<cnt<<endl;
		}
	}

// 特别的，n只会有一个大于sqrt（n）的质数要是有俩的话相乘就会大于n了

	if(num>1) cout<<num<<" "<<1<<endl;
```

- 质数筛

<details>
    <summary> <font color="blue">本人听课再次推理心得</font> </summary>
	<p>首先呢，朴素筛法就是枚举1-n，把所有1-n的倍数全部筛出去，留下的数就是质数，时间复杂度O（n^2）</p>
	我们就可以用优化筛法，把所有质数的倍数全部筛出去，因为所有的合数都会有一个质因子，这是一个定理，这样的话相当于直筛质数的倍数那我们思考一下，1~n中有多少个质数呢，有n/ln（n）个质数，那时间复杂度大概就是O(nlogn) <br> 还可以在优化，有的合数会被不同的质数筛掉例如12会被2和3筛掉，我们能不能想到一些只会被一个质数筛掉的方法呢，有的兄弟有的当然是我们伟大的最小质因子啦，我们只需要从小到大枚举质数，只让最小的质数的倍数标记为合数，然后如果当前数能被这个质数整除，说明当前数的最小质因子就是他，那我们就break即可<br>
	下面有两种分类判断<br>
	if i%pj == 0 说明i的最小质因数是pj，则i*pj的最小质因数全是pj<br>
    if i%pj != 0 说明i没有找到最小的质因子，还没又出现，那这种情况i*pj的最小质因子依旧是pj呀<br>
    至此全部证毕故所有数字只会被筛一次，时间复杂度O(n);<br>
</details>


```cpp
	vector<int> primes;
	vector<int> v(10000005,0);
	
	for(int i=2;i<=n;i++){
		if(!v[i]) primes.push_back(v[i]);
		for(int j=0;primes[j] <= n/i;j++){
			v[primes[j]*i] = 1;
			if(i%primes[j]==0) break;
		}
	}
```
### 6. 约数
- 试除法求约数 

<details> 
	<summary> <font color="blue"> 本人听课再次推理心得 </font> </summary>
	首先呢<br>
	对于所有数，他的约数都是成对存在的，除了哪个平方，所以我们可以直接同理sqrt，特判一下，就ok了<br>
</details>

```cpp
	vector<int> res;
	
	for(int i=1;i<=n/i;i++){
		if(n%i==0){
			res.push_back(i);
			if(i!=n/i) res.push_back(n/i);
		}
	}

```
- 约数个数以及和

#### 性质 

>令$N$ 为数，$P$ 为质因数，$\alpha$ 为质因数指数 
>
>则有
>
>$N = P_1^{\alpha_1} P_2^{\alpha_2}P_3^{\alpha_3}...P_k^{\alpha_k}$
>
>令 $d$ 为约数
>
>$d=P_1^{\beta_1}P_2^{\beta_2}P_3^{\beta_3}...P_k^{\beta_k}$
>
>可以理解为d就是N的一个子集，那数量一共就有
>
>$(\alpha_1+1)(\alpha_2+1)(\alpha_3+1)...(\alpha_k+1)$
>
>那么相应的约数的和就是这些$d$加在一起，答案为
>
>$(P_1^0+P_1^1+...+P_1^{\alpha_1})...(P_k^0+P_k^1+...+P_k^{\alpha_k})$
>
>请自行拆开便能理解

<font color="red"> 特别的</font> int 范围内约数最多的数的个数大概在1500-1600

### 7.辗转相除求最大公约数

```cpp
	return b?gcd(b,a%b):a;
```

### 8. 欧拉函数

![image-20260511193606451](C:\Users\Lenovo\AppData\Roaming\Typora\typora-user-images\image-20260511193606451.png)

```cpp

        int num;
        cin>>num;
        
        int res = num;
        
        for(int i=2;i<=num/i;i++){
            if(num%i==0){
                while(num%i==0){
                    num/=i;
                }
                res = (long long)res*(i-1)/i;
            }
        }
        
        if(num>1) res = (long long) res*(num-1)/num;
```

### 9. 高斯消元
> 步骤为 
> 枚举每一列
> 找到绝对值最大主元
> 将主元变为1
> 将该列以下数字变为0

```cpp
#include<bits/stdc++.h>
using namespace std;
const int N = 110;
const double eps = 1e-10;
int n;
double a[N][N];


int main(){
    cin>>n;
    for(int i=0;i<n;i++)
        for(int j=0;j<=n;j++)
            cin>>a[i][j];
    
    auto guass = [&]()->int{
        int r,c;
        for(c=0,r=0;c<n;c++){
            int t = r;
            for(int i=r;i<n;i++)
                if(fabs(a[i][c])>fabs(a[t][c])) 
                    t = i;
            
            if(fabs(a[t][c])<eps) continue;
            
            for(int i=c;i<=n;i++) swap(a[r][i],a[t][i]);
            
            for(int i=n;i>=c;i--) a[r][i]/=a[r][c];
            
            for(int i=r+1;i<n;i++)
                if(fabs(a[i][c])>eps)
                    for(int j=n;j>=c;j--)
                        a[i][j] -= a[r][j]*a[i][c];
                    
                
            r++;
        }
        
        if(r<n){
            for(int i=r;i<n;i++)
                if(fabs(a[i][n])>eps)
                    return 1;
            
            return 2;
        }
        
        for(int i=n-1;i>=0;i--){
            for(int j=i+1;j<n;j++){
                a[i][n]=a[i][n]-a[i][j]*a[j][n];
            }
        }
        
        return 0;
    } ;  
    
    int d = guass();
    
    if(d==0) for(int i=0;i<n;i++) printf("%.2lf\n",a[i][n]);
    else if(d==2) puts("Infinite group solutions");
    else puts("No solution");
    
    return 0;
}
```
### 10.组合计数
> 当a,b范围较小的时候可以用递推求出来
> 

```cpp
void init(){
    for(int i=0;i<=2000;i++){
        for(int j=0;j<=i;j++){
            if(!j) dp[i][j] = 1;
            else dp[i][j] = (dp[i-1][j] + dp[i-1][j-1])%mod;
        }
    }
}
```
>   a,b范围较大时候可以从定义出发求出a的阶乘和b的阶乘逆元和c的阶乘逆元即可
>   

```cpp
    fact[0] = 1;
    for(int i=1;i<=100000;i++) fact[i]=(long long )i*fact[i-1]%mod;
    for(int i=0;i<=100000;i++) infact[i]=(long long)qmi(fact[i],mod-2,mod)%mod;
```

> 卢卡斯定理求组合数，适用于a,b极大的数据范围
> 卢卡斯定理 ${a\choose b} = {a mod p\choose bmodp} * {a/p\choose b/p}$

``` cpp
	auto qmi = [](LL a,LL b,LL p){
        int res = 1%p;
        a =a%p;
        
        for(;b;b>>=1){
            if(b&1) res =(LL) res * a%p;
            a = (LL)a*a%p;
        }
        
        return res;
    };
    
    auto C = [&](LL a,LL b,LL p){
        int res = 1%p;
        
        if(a<b) return 0;
        
        for(int i=a,j=1;j<=b;j++,i--){
            res = (LL) res * i %p;
            res = (LL) res * qmi(j,p-2,p)%p;
        }
        
        return res;
    };
    
    auto Lucus = [&](auto self,LL a,LL b,LL p)->int{
        if(a<p&&b<p) return C(a,b,p);
        return (LL)C(a%p,b%p,p)*self(self,a/p,b/p,p)%p;
    };
```

> 遇到高精度写法的时候，可以进行拆分质因子，让上面的质因子减去下面的质因子，最终让答案累乘质因子即可

```cpp
#include<bits/stdc++.h>
using namespace std;
const int N = 5010;
int prime[N],cnt,sum[N];
bool v[N];

void get_prime(int n){
    for(int i=2;i<=n;i++){
        if(!v[i]) prime[++cnt] = i;
        for(int j=1;prime[j]<=n/i;j++){
            v[prime[j]*i] = 1;
            if(i%prime[j]==0) break;
        }
    }
}

int main(){
    int a,b;
    cin>>a>>b;
    get_prime(a);
    
    auto get = [](int n,int p){
        int res = 0;
        while(n){
            res+=n/p;
            n/=p;
        }
        
        return res;
    };
    
    for(int i=1;i<=cnt;i++){
        int p = prime[i];
        sum[i] = get(a,p)-get(b,p)-get(a-b,p);
    }
    
    vector<int> ans;
    ans.push_back(1);
    
    auto mul = [](vector<int> a,int b){
        vector<int> c;
        int t =0;
        for(int i=0;i<a.size();i++){
            t += b*a[i];
            c.push_back(t%10);
            t/=10;
        }
        
        while(t){
            c.push_back(t%10);
            t/=10;
        }
        
        return c;
    };
    
    for(int i=1;i<=cnt;i++)
        for(int j=0;j<sum[i];j++)
            ans = mul(ans,prime[i]);
            
    for(int i=ans.size()-1;i>=0;i--) cout<<ans[i];
    
    puts("");
    
    return 0;
}
```

> 卡特兰数列，适用于栈的顺序个数啥的，核心思想是将合法顺序转换为图的形式，采取对称，将所有不合法的顺序找出即可，答案为$\frac{2n \choose n}{n+1}$

### 11.博弈论
> 简单博弈论的求sg函数
> 

```cpp
#include<bits/stdc++.h>
using namespace std;
const int N = 110,M = 10010;

int n,m;
int s[N],f[M];

int sg(int x){
    if(f[x]!=-1) return f[x];
    
    unordered_set<int> S;
    
    for(int i=1;i<=m;i++){
        int sum = s[i];
        if(x>=sum) S.insert(sg(x-sum));
    }
    
    for(int i=0;;i++){
        if(!S.count(i)){
            return f[x]=i;
        }
    }
}

int main(){
    cin>>m;
    
    for(int i=1;i<=m;i++) cin>>s[i];
    
    cin>>n;
    
    memset(f,-1,sizeof(f));
    
    int res = 0;
    for(int i=0;i<n;i++){
        int x;
        cin>>x;
        res^= sg(x);
    }
    
    if(res) puts("Yes");
    else puts("No");
    
    return 0;
}
```

---

## 五、 考场通关与刷题心法 

- [ ] **翻译题目**：强迫自己用一句话、严谨的数学语言重新描述题意，剥离冗余背景。
- [ ] **极限施压**：要想出能卡掉程序的**极限数据与边界**，穷举所有极端情况，防止漏判。
- [ ] **拒绝干瞪眼**：遇到抽象限制，必须**纸笔手工模拟**，自己去造数据理解流程。
- [ ] **复杂度反推**：从数据范围反推时间复杂度（例如看到 $10^5$ 盲猜 $O(N \log N)$），再去对应层级的算法和数据结构里找武器。
- [ ] **避坑指南**：字符串哈希容易冲突且极难调试，能用 `std::map` 转化为数字 ID 的，就优先转成数字 ID！
- [ ] **查询越界思维**：用 `upper_bound - begin()` 求 $\le target$ 的个数时，算出来的本质是“越界者的排名”，直接用 `ask(rank - 1)` 查询，逻辑闭环最严密。
- [ ] 数组太大导致MLE可以考虑使用bitset代替bool数组，就是访问慢了点

## 六、 矩阵与坐标技巧

**主副对角线恒等式：**

- 主对角线（左上-右下）：$i - j = \text{const}$。
- 副对角线（右上-左下）：$i + j = \text{const}$。

**坐标转换应用：** 在 `X-Sum` 题中，利用此规律可将 $O(N^2)$ 的动态搜索优化为 $O(1)$ 的预处理读取。

---

## 附录：实战长代码留档

### 【实战例题：配对统计】完整扫描线代码
```cpp
#include <bits/stdc++.h>
#define int long long
using namespace std;
const int N = 3e5+10;

int test, n, m;
int f[N];

void add(int x, int y) {
	for (; x <= n; x += x & (-x)) f[x] += y;
}

int ask(int x) {
	int res = 0;
	for (; x; x -= x & (-x)) res += f[x];
	return res;
}

void solve() {
	cin >> n >> m;
	if (n == 1) {
		cout << "0\n";
		return;
	}
	vector<pair<int, int>> a(n + 1);
	for (int i = 1; i <= n; i++) {
		cin >> a[i].first;
		a[i].second = i;
	}

	sort(a.begin() + 1, a.end(), [](const auto & aa, const auto & bb) {
		return aa.first < bb.first;
	});

	vector<pair<int, int>> pairs;

	// 处理首尾情况
	int l = min(a[1].second, a[2].second), r = max(a[1].second, a[2].second);
	pairs.push_back({l, r});
	l = min(a[n].second, a[n - 1].second), r = max(a[n].second, a[n - 1].second);
	pairs.push_back({l, r});

	for (int i = 2; i < n; i++) {
		int ldif = a[i].first - a[i - 1].first;
		int rdif = a[i + 1].first - a[i].first;
		if (ldif < rdif) {
			int x = a[i].second, y = a[i - 1].second;
			if (x > y) swap(x, y);
			pairs.push_back({x, y});
		} else if (ldif == rdif) {
			int x1 = a[i].second, y1 = a[i - 1].second;
			int x2 = a[i].second;
			int y2 = a[i + 1].second;
			if (x1 > y1) swap(x1, y1);
			if (x2 > y2) swap(x2, y2);
			pairs.push_back({x1, y1});
			pairs.push_back({x2, y2});
		} else {
			int x = a[i].second, y = a[i + 1].second;
			if (x > y) swap(x, y);
			pairs.push_back({x, y});
		}
	}

	sort(pairs.begin(), pairs.end(), [](const auto & aa, const auto & bb) {
		if (aa.second != bb.second) return aa.second < bb.second;
		return aa.first < bb.first;
	});

	int ans = 0, pair_idx = 0;
	int total_pairs = pairs.size();

	vector<pair<int, pair<int, int> > > dicar;
	for (int i = 1; i <= m; i++) {
		int l, r;
		cin >> l >> r;
		dicar.push_back({i, {l, r}});
	}

	sort(dicar.begin(), dicar.end(), [](const auto & aa, const auto & bb) {
		return aa.second.second < bb.second.second;
	});

    // 扫描线核心逻辑
	for (auto &it : dicar) {
		while (pair_idx < total_pairs && pairs[pair_idx].second <= it.second.second) {
			add(pairs[pair_idx].first, 1);
			pair_idx++;
		}
		int lef = ask(it.second.first - 1);
		int cnt = pair_idx - lef;
		ans += cnt * it.first;
	}

	cout << ans << '\n';
}

signed main() {
	ios::sync_with_stdio(0); cin.tie(0);
	solve();
	return 0;
}
```

